using UnityEngine;
using System.Collections.Generic;
using UnityEngine.Playables;

public class VNLDefines : MonoBehaviour
{
    public readonly TextAsset[] VNLScripts;
    public readonly Dictionary<string, PlayableDirector> VNLDirectors;
    public readonly Dictionary<string, (string localName, string style)> Characters;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
